#!/bin/sh

# Code search using dmenu, ag (single string)

cd "$HOME" || exit 1
result=`ag "$1" | dmenu -p "Select file: "`

if test -n "$result"; then
	subres="${result#*:}"
	edit +"${subres%%:*}" "$HOME/${result%%:*}" &
fi
exit 0
