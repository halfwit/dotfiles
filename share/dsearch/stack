#!/usr/bin/env bash
# Handler to push window to top of stack

lsw -a | while read -r id; do
  if wname "$id" | grep -a "$1"; then
    mapw -m "$id"
    printf '%s' "$id" > /tmp/groups/group.2
  elif wname "$id" | grep -a "surf"; then
    mapw -u "$id"
  fi
done
