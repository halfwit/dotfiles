section: screens
	lapdog:
	desky:
	lappy:
end

section: links
	desky:
		left = lappy
		right = lapdog
	lapdog:
		left = desky
	lappy:
		right = desky
end

section: options
	keystroke(alt+super+h) = switchInDirection(left)
	keystroke(alt+super+l) = switchInDirection(right)
end
