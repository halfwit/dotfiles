#!/usr/bin/env bash

if ! pgrep mpv; then
  exec mpv --input-unix-socket=/tmp/mpvsocket --keepaspect-window "$1"
fi

echo ' { "command": ["loadfile", "'"$1"'", "append-play"] }' | socat - /tmp/mpvsocket
