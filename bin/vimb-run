#!/usr/bin/env bash
# Script to open Vimb instances in tabbed
if ! pgrep vimb; then
    tabbed -u \#ebdbb2 -U \#d5c4a1 -t \#7c6f64 -T \#f9f5d7 -c -d >/tmp/tabbed.xid
fi

exec vimb -e "$(</tmp/tabbed.xid)" "$1"
