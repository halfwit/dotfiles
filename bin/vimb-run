#!/usr/bin/env bash
# Script to open Vimb instances in tabbed
if ! pgrep vimb; then
    tabbed -U \#ebdbb2 -u \#504945 -T \#ebdbb2 -t \#32302f -c -d -p 2 >/tmp/tabbed.xid
fi

exec vimb -e "$(</tmp/tabbed.xid)" "$@"
