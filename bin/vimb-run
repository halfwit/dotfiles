#!/usr/bin/env bash
# Script to open Vimb instances in tabbed
if ! pgrep vimb; then
    tabbed -u \#eee8d5 -U \#839496 -t \#657b83 -T \#fdf6e3 -c -d -p 2 >/tmp/tabbed.xid
fi

exec vimb -e "$(</tmp/tabbed.xid)" "$@"
