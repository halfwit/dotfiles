#!/usr/bin/env bash
readonly TERMINAL=termite

if ! pgrep nvim; then
    exec termite --exec="/usr/bin/nvim $1"
else

python - <<END
from neovim import attach
nvim = attach('socket', path='/tmp/nvimsocket')
nvim.command('e $1')
END

fi
